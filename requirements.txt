# Runtime dependencies
fastapi~=0.115.0
uvicorn[standard]~=0.32.0
pydantic~=2.9.0
jinja2~=3.1.4

# Test dependencies (not required to run the app in production,
# kept here for simplicity of a single-file Render build step)
pytest~=8.3.0
httpx~=0.27.0